---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/fonts/Baskervville-VariableFont_wght.woff2
  - public/fonts/Baskervville-Italic-VariableFont_wght.woff2
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [BRAND-01, BRAND-02]

must_haves:
  truths:
    - "Every page renders in Baskervville with no flash of unstyled text on initial load"
    - "Brand color utility classes (bg-gold, text-navy, bg-slate, text-ivy) are available and functional"
    - "The body text color defaults to slate (#1a1b1b) instead of browser default black"
  artifacts:
    - path: "public/fonts/Baskervville-VariableFont_wght.woff2"
      provides: "Variable font file for normal style (all weights)"
    - path: "public/fonts/Baskervville-Italic-VariableFont_wght.woff2"
      provides: "Variable font file for italic style (all weights)"
    - path: "src/styles/global.css"
      provides: "@theme tokens for colors and font, @font-face declarations in @layer base"
      contains: "--color-gold: #ebbc28"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Font preload links, updated meta description, AnnouncementBar removed"
      contains: "rel=\"preload\""
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "public/fonts/"
      via: "<link rel='preload'> tags referencing /fonts/*.woff2"
      pattern: "preload.*Baskervville"
    - from: "src/styles/global.css"
      to: "public/fonts/"
      via: "@font-face url() declarations"
      pattern: "url.*fonts/Baskervville"
    - from: "src/styles/global.css"
      to: "Tailwind utility generation"
      via: "@theme block with --color-* and --font-* tokens"
      pattern: "--color-gold.*#ebbc28"
---

<objective>
Establish the brand design foundation: convert fonts to WOFF2, define Tailwind v4 theme tokens for brand colors and typography, configure font preloading, and update the base layout.

Purpose: Everything in this phase depends on brand tokens and fonts being available. This plan creates the foundation that Header, Footer, and micro-interaction plans build upon.
Output: WOFF2 font files in public/fonts/, branded global.css with @theme tokens and @font-face rules, updated BaseLayout.astro with preloading and cleaned-up meta.
</objective>

<execution_context>
@/Users/alin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-RESEARCH.md

@src/styles/global.css
@src/layouts/BaseLayout.astro
@src/components/AnnouncementBar.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert fonts to WOFF2 and define brand theme tokens</name>
  <files>public/fonts/Baskervville-VariableFont_wght.woff2, public/fonts/Baskervville-Italic-VariableFont_wght.woff2, src/styles/global.css</files>
  <action>
1. Create `public/fonts/` directory.

2. Convert the two TTF variable font files to WOFF2 using fonttools:
   ```
   pip install fonttools brotli (if not already installed)
   python -m fonttools.ttLib.woff2 compress "assets/fonts/Baskervville.zip (Unzipped Files)/Baskervville-VariableFont_wght.ttf" -o public/fonts/Baskervville-VariableFont_wght.woff2
   python -m fonttools.ttLib.woff2 compress "assets/fonts/Baskervville.zip (Unzipped Files)/Baskervville-Italic-VariableFont_wght.ttf" -o public/fonts/Baskervville-Italic-VariableFont_wght.woff2
   ```

3. Rewrite `src/styles/global.css` with this structure (preserve the existing shake animation keyframes):

   ```css
   @import "tailwindcss";

   /* 1. Theme tokens — top-level, NOT inside @layer */
   @theme {
     --color-gold: #ebbc28;
     --color-navy: #052a38;
     --color-slate: #1a1b1b;
     --color-ivy: #00442e;

     --font-baskervville: "Baskervville", "Georgia", "Times New Roman", serif;
     --default-font-family: var(--font-baskervville);

     --animate-shake: shake 0.5s infinite;

     @keyframes shake {
       0% { transform: translate(1px, 1px) rotate(0deg); }
       10% { transform: translate(-1px, -2px) rotate(-1deg); }
       20% { transform: translate(-3px, 0px) rotate(1deg); }
       30% { transform: translate(3px, 2px) rotate(0deg); }
       40% { transform: translate(1px, -1px) rotate(1deg); }
       50% { transform: translate(-1px, 2px) rotate(-1deg); }
       60% { transform: translate(-3px, 1px) rotate(0deg); }
       70% { transform: translate(3px, 1px) rotate(-1deg); }
       80% { transform: translate(-1px, -1px) rotate(1deg); }
       90% { transform: translate(1px, 2px) rotate(0deg); }
       100% { transform: translate(1px, -2px) rotate(-1deg); }
     }
   }

   /* 2. Font declarations in @layer base */
   @layer base {
     @font-face {
       font-family: "Baskervville";
       src: url("/fonts/Baskervville-VariableFont_wght.woff2") format("woff2");
       font-weight: 100 900;
       font-style: normal;
       font-display: swap;
     }

     @font-face {
       font-family: "Baskervville";
       src: url("/fonts/Baskervville-Italic-VariableFont_wght.woff2") format("woff2");
       font-weight: 100 900;
       font-style: italic;
       font-display: swap;
     }

     html {
       @apply scroll-smooth;
     }

     body {
       @apply flex min-h-screen flex-col bg-white text-slate;
     }

     main {
       @apply flex-1;
     }

     details > summary { list-style: none; }
     details > summary::-webkit-details-marker { display: none; }

     *,
     :before,
     :after {
       @apply border-gray-200;
     }
   }

   /* 3. Container utility */
   @utility container {
     margin-inline: auto;
     padding-inline: 1.5rem;
     max-width: 80rem;
   }
   ```

   IMPORTANT: Do NOT put @theme inside @layer — it must be top-level or Tailwind v4 will not generate utility classes. Do NOT include the .button class yet — that goes in Plan 03 (micro-interactions).
  </action>
  <verify>
    <automated>cd /Users/alin/code/work/abc-dealtoys && test -f public/fonts/Baskervville-VariableFont_wght.woff2 && test -f public/fonts/Baskervville-Italic-VariableFont_wght.woff2 && pnpm build</automated>
  </verify>
  <done>Two WOFF2 files exist in public/fonts/. global.css has @theme with --color-gold, --color-navy, --color-slate, --color-ivy, --font-baskervville, and --default-font-family. @font-face rules reference WOFF2 files with font-weight: 100 900 and font-display: swap. `pnpm build` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update BaseLayout with font preloading and cleanup</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
Update `src/layouts/BaseLayout.astro`:

1. Remove the `AnnouncementBar` import and its `<AnnouncementBar />` usage from the body.

2. Add font preload links BEFORE the `<style is:global>` tag in `<head>`:
   ```astro
   <link rel="preload" href="/fonts/Baskervville-VariableFont_wght.woff2" as="font" type="font/woff2" crossorigin />
   <link rel="preload" href="/fonts/Baskervville-Italic-VariableFont_wght.woff2" as="font" type="font/woff2" crossorigin />
   ```
   The `crossorigin` attribute is REQUIRED — without it, the browser fetches the font twice (once for preload, once for @font-face).

3. Update the default description to: "Custom deal toys and financial tombstones for closers."

4. Update the title format to: `{title} | ABC Deal Toys`

5. Add `initial-scale=1` to the viewport meta tag: `content="width=device-width, initial-scale=1"`

Keep all other existing imports (Header, Footer, CartDrawer) and structure intact.
  </action>
  <verify>
    <automated>cd /Users/alin/code/work/abc-dealtoys && pnpm build && grep -q 'preload' src/layouts/BaseLayout.astro && grep -q 'crossorigin' src/layouts/BaseLayout.astro && ! grep -q 'AnnouncementBar' src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>BaseLayout.astro has two font preload links with crossorigin attribute. AnnouncementBar import and usage removed. Title format is "{title} | ABC Deal Toys". Default description updated. Viewport meta includes initial-scale=1. `pnpm build` passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- WOFF2 files exist at public/fonts/ and are smaller than the source TTF files
- global.css contains @theme block at top level (not nested in @layer)
- BaseLayout has preload links before the stylesheet
- No references to AnnouncementBar remain in BaseLayout
</verification>

<success_criteria>
- Brand font loads on every page via Baskervville variable font in WOFF2 format
- Brand colors available as Tailwind utilities (bg-gold, text-navy, text-slate, bg-ivy, etc.)
- Font preloading eliminates FOUT in production builds
- `pnpm build` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md`
</output>
