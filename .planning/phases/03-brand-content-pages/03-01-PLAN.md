---
phase: 03-brand-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/about.astro
  - src/pages/how-it-works.astro
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "A visitor can navigate to /about and read the company's mission, brand narrative, and credibility signals"
    - "A visitor can navigate to /how-it-works and understand the 3-step process (browse, customize, order) with realistic timeline expectations"
    - "Both pages use the existing BaseLayout with Header and Footer"
    - "Both pages are mobile-responsive and use brand typography and colors"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "About / Our Story page"
      contains: "BaseLayout"
    - path: "src/pages/how-it-works.astro"
      provides: "How It Works process page"
      contains: "BaseLayout"
  key_links:
    - from: "src/pages/about.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import and wrapping"
      pattern: "import BaseLayout"
    - from: "src/pages/how-it-works.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import and wrapping"
      pattern: "import BaseLayout"
---

<objective>
Create the About / Our Story page and How It Works page as static Astro pages wrapped in BaseLayout.

Purpose: Fill out the site's informational content skeleton so visitors understand who ABC Deal Toys is and how the ordering process works.
Output: Two new Astro pages at /about and /how-it-works, fully styled with brand colors and typography.
</objective>

<execution_context>
@/Users/alin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/layouts/BaseLayout.astro
@src/pages/index.astro
@src/styles/global.css
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/layouts/BaseLayout.astro:
```typescript
export interface Props {
  title: string;
  description?: string;
}
```

From src/styles/global.css (Tailwind v4 theme tokens):
```css
@theme {
  --color-gold: #ebbc28;
  --color-navy: #052a38;
  --color-slate: #1a1b1b;
  --color-ivy: #00442e;
  --font-baskervville: "Baskervville", "Georgia", "Times New Roman", serif;
}
```

Tailwind utility classes available: `text-gold`, `text-navy`, `text-slate`, `text-ivy`, `bg-gold`, `bg-navy`, `bg-slate`, `bg-ivy`, `font-baskervville`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create About / Our Story page</name>
  <files>src/pages/about.astro</files>
  <action>
Create `src/pages/about.astro` wrapped in BaseLayout with title "About Us | ABC Deal Toys" and a description meta tag.

Page content structure (all hardcoded — no API calls, no Shopify data):

1. **Hero section**: Full-width banner with bg-navy, centered headline "Our Story" in text-gold, subheadline introducing ABC Deal Toys.

2. **Mission section**: "Celebrate the closers" brand narrative. Explain that ABC Deal Toys crafts premium custom deal toys for investment bankers, lawyers, and professionals who close landmark transactions. Tone: confident, premium, slightly irreverent (inspired by "Coffee's for closers" energy). Use a two-column layout on desktop (text left, decorative accent right) that stacks vertically on mobile.

3. **Credibility section**: 3-column grid (1 col on mobile) with key differentiators:
   - "Precision Craftsmanship" — each piece is custom-designed
   - "Industry Expertise" — understanding of deal tombstone traditions
   - "Fast Turnaround" — streamlined ordering to delivery process
   Each item gets an icon placeholder (use an SVG circle or simple geometric shape as icon placeholder — do NOT add an external icon library), a heading, and 2-3 sentences of copy.

4. **CTA section**: bg-navy band with gold headline "Ready to celebrate your next close?" and a link styled as a button (bg-gold text-navy font-semibold px-8 py-3 rounded) pointing to "/contact".

Use the `container` utility class (defined in global.css) for horizontal padding and max-width. Use `py-16 md:py-24` for vertical section spacing. All text should use the default Baskervville font (already set globally).

Mobile-responsive: Use Tailwind responsive prefixes (md:, lg:) for grid columns and text sizes. Ensure minimum 16px body text.
  </action>
  <verify>
    <automated>cd /Users/alin/code/work/abc-dealtoys && pnpm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Visiting /about renders a styled About page with mission narrative, credibility section, and CTA — all using brand colors and responsive layout</done>
</task>

<task type="auto">
  <name>Task 2: Create How It Works page</name>
  <files>src/pages/how-it-works.astro</files>
  <action>
Create `src/pages/how-it-works.astro` wrapped in BaseLayout with title "How It Works | ABC Deal Toys" and a description meta tag.

Page content structure (all hardcoded — no API calls):

1. **Hero section**: Full-width bg-navy banner with centered headline "How It Works" in text-gold and a brief subheadline: "From browsing to unboxing — your custom deal toy in three simple steps."

2. **Steps section**: Three-step process displayed as numbered cards in a responsive grid (3 cols on lg, 1 col on mobile). Each step card:

   - **Step 1: Browse & Choose** — "Explore our collection of premium deal toy templates. Each design is crafted to reflect the prestige of your transaction." Link to "/" (shop).
   - **Step 2: Customize Your Details** — "Add your deal specifics — transaction name, parties involved, closing date, and deal value. Upload your firm's logo to make it truly yours."
   - **Step 3: Place Your Order** — "Review your customization, add to cart, and check out securely through Shopify. We handle the rest."

   Each card: bg-white with shadow-md, rounded-lg, p-8. Step number displayed as a large (text-5xl) text-gold numeral at the top. Heading in text-navy font-semibold text-xl. Description in text-slate/80.

3. **Timeline expectations section**: A centered text block below the steps. Heading: "What to Expect". Content: "Standard orders ship within 2-3 weeks of order confirmation. Rush orders may be available — contact us for details." Style with a subtle top border (border-t border-gold/30) and generous padding.

4. **CTA section**: Same pattern as About page — bg-navy band with "Ready to get started?" headline and two buttons: "Browse Collection" (bg-gold text-navy, links to "/") and "Get a Quote" (border border-gold text-gold, links to "/contact").

Use `container` utility for horizontal constraints. Same vertical spacing pattern as About page (py-16 md:py-24 per section).
  </action>
  <verify>
    <automated>cd /Users/alin/code/work/abc-dealtoys && pnpm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Visiting /how-it-works renders a styled process page with 3-step cards, timeline expectations, and CTAs — all responsive and using brand tokens</done>
</task>

</tasks>

<verification>
- `pnpm run build` completes without errors
- `pnpm run typecheck` passes
- Both /about and /how-it-works routes are generated in the build output
</verification>

<success_criteria>
- /about page renders company mission, credibility signals, and CTA with brand styling
- /how-it-works page renders 3-step process, timeline, and CTAs with brand styling
- Both pages are mobile-responsive (stack to single column on small viewports)
- Both pages use BaseLayout (Header + Footer visible)
- No external dependencies added (pure Astro + Tailwind)
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-content-pages/03-01-SUMMARY.md`
</output>
